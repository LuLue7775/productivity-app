import { createContext } from "react";

export const ModalContext = createContext(); //login or timer
export const ThemeContext = createContext(); 
export const TimerContext = createContext();
export const MissionsContext = createContext(); // all missions across the app